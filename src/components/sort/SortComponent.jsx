import classes from './SortComponent.module.css';
import BarList from './BarList';

function SortComponent() {
  return (
    <main className={classes.sort}>
      <BarList />
    </main>
  );
}

export default SortComponent;
